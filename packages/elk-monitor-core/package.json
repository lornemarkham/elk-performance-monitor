{
  "name": "elk-monitor-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared event contract for ELK Performance Monitor (parent, iframe, extension).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  }
}
